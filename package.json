{
	"name": "ai-pr-review",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:playwright": "wrangler deploy -c wrangler-playwright.jsonc",
		"dev": "wrangler dev",
		"dev:playwright": "wrangler dev -c wrangler-playwright.jsonc",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"vitest": "~3.2.0",
		"wrangler": "^4.71.0"
	}
}